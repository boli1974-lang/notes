// Review service: business logic for Review Today (batch selection, mark reviewed).
// Uses repositories only; no Prisma calls directly in this module.

// export async function getOrCreateTodayBatch(userId: string | null): Promise<...> { ... }
// export async function markNoteReviewed(noteId: string, ...): Promise<void> { ... }
